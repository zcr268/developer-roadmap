# Crawling, Indexing, Ranking
 
Crawling is when a search engine's bots visit and read pages on the web. Indexing is the process of storing and organizing that content so it can be retrieved later. Ranking is the final step, where the search engine orders indexed pages by relevance and quality for a specific query.