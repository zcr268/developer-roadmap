# Domain & Protocol Changes
 
A domain change is moving a website to an entirely new domain name. A protocol change is switching between HTTP and HTTPS, or between www and non-www versions of a URL. Both require careful redirect handling so search engines and users are correctly sent to the new location.