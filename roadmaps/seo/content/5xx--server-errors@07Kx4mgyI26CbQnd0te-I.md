# 5xx & Server Errors
 
A 5xx status code indicates a server-side error, meaning the problem is with the website's server rather than the request itself. Frequent 5xx errors during crawling can cause search engines to slow down or reduce how often they crawl a site, since it signals the server may be unreliable.