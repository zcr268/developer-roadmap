# API & Feed Access
 
Some AI platforms and services pull data directly through structured feeds or APIs, rather than crawling a page the way a browser would. This node covers making sure that data is accessible through those channels too, since a site can be technically well optimized for crawling and still be invisible to a system that expects a feed instead.