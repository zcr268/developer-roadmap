# API Reference

An API reference is a complete, structured description of every endpoint, parameter, request format, and response a web API exposes. It gives developers the precise technical detail they need to make calls and handle responses correctly. API references are typically generated from code or specification files like OpenAPI and kept in sync with the live API.