# OpenGraph Data

OpenGraph data is metadata embedded in a web page's HTML that controls how the page appears when shared on social media platforms. It defines the title, description, and image that platforms like LinkedIn, X, and Slack display in link previews. Technical writers and content teams configure OpenGraph tags to ensure shared content looks accurate and appealing.