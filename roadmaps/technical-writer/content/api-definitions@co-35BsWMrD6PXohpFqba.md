# API Definitions

API definitions are machine-readable descriptions of an API's structure, endpoints, parameters, and responses. Formats like OpenAPI (formerly Swagger), RAML, and GraphQL schemas are common standards. Technical writers work with API definitions to generate reference docs, validate examples, and ensure consistency between documentation and the actual API behavior.