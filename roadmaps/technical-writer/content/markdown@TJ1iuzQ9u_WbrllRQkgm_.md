# Markdown

Markdown is a lightweight markup language that uses plain text formatting syntax to define headings, lists, code blocks, links, and other elements. It is widely used in technical documentation because it is readable as plain text and converts easily to HTML. Most modern documentation platforms and static site generators support Markdown natively.