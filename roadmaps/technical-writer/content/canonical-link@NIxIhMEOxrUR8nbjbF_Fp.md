# Canonical Link

A canonical link is an HTML tag that tells search engines which version of a page is the authoritative one when similar or duplicate content exists at multiple URLs. Technical writers and SEO teams use canonical tags to prevent duplicate content from fragmenting search rankings. Proper canonicalization is especially relevant when content is syndicated across multiple platforms.