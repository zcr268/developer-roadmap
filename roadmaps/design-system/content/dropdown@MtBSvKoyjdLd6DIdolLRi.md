# Dropdown

A dropdown is a navigation or selection component that presents a list of options when a user interacts with a toggle element, such as a button or an icon. It serves as an efficient way to save screen space by hiding secondary choices until they are explicitly needed. When activated, the component typically displays a menu overlay containing actionable items, filters, or navigational links that close once a selection is made or the user clicks outside the menu area.