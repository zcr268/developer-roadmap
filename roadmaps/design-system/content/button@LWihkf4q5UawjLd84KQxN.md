# Button

A button is an interactive element that triggers an action or event when a user clicks or taps on it. It serves as a primary navigation or functional trigger within a user interface, often featuring distinct styles to represent different states such as hover, active, or disabled. By defining a set of consistent properties like shape, typography, and color, buttons ensure predictable user interactions across an entire application.