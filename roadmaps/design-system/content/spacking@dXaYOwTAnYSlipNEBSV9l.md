# Spacing

Spacing tokens define the standard set of gap and padding values used between and within components, usually built as a scale of multiples of a base unit. Having a fixed spacing scale prevents designers and developers from picking arbitrary pixel values, which keeps layouts visually consistent. Components reference spacing tokens for their internal padding and the gaps between elements.