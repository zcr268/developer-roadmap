# Sizes

Icon sizes define the standard dimensions an icon set supports, such as 16px, 24px, and 32px, so icons remain crisp and consistent wherever they appear. Design systems typically build icons on a fixed grid at each supported size to keep stroke widths and proportions consistent across the set. Components reference these standard sizes rather than scaling icons arbitrarily.