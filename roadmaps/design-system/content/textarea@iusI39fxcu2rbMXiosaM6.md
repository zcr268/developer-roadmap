# Textarea

A textarea component is a multi-line text input used for longer form entries, such as comments or descriptions, as opposed to a single-line text input. It usually supports resizing, character count limits, and validation states like error or success. Textareas share styling conventions with other input components to keep form fields visually consistent.