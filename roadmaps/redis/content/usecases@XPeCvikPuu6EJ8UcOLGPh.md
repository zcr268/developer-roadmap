# Usecases

HyperLogLog is suited for counting unique visitors, search queries, or distinct events at scale where approximate counts are acceptable. It is far more memory-efficient than storing actual sets of unique values. Common use cases include daily unique user counts, analytics dashboards, and deduplication estimation.