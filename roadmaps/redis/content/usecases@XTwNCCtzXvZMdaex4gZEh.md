# Usecases

Lists are commonly used to implement task queues, activity feeds, and chat message histories. The ability to push and pop from both ends makes them flexible for FIFO queues and LIFO stacks. Blocking pop commands enable lightweight worker queue patterns without polling.