# Usecases

Sets are used to track unique visitors, manage tags or labels, implement friend lists, and enforce uniqueness constraints. Set operations like intersection and difference are particularly useful for computing shared connections or filtering audiences. The O(1) membership test makes Sets efficient for blocklists and allowlists.