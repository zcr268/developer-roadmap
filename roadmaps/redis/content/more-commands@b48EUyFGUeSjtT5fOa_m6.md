# More Commands - Sets

Additional Set commands include `SPOP` for removing and returning a random member, `SRANDMEMBER` for returning random members without removal, and `SMOVE` for atomically moving a member from one set to another. These support use cases like random sampling and atomic transfers.

Visit the following resources to learn more:

- [@official@All Redis Set Commands](https://redis.io/docs/latest/commands/?group=set)