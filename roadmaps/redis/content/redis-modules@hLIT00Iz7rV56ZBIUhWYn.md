# Redis Modules

Redis Modules are extensions that add new data types and commands to Redis. They are loaded at startup via `redis.conf` or the `MODULE LOAD` command. Modules allow Redis to be extended for specialized use cases without modifying the core server.