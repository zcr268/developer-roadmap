# Subnets

Subnets divide a VPC's IP address range into smaller segments, each tied to a specific availability zone. Resources in a subnet inherit its routing configuration, which determines whether they can reach the internet or stay private. A VPC can have many subnets spread across multiple availability zones.

Visit the following resources to learn more:

- [@official@Subnets](https://docs.aws.amazon.com/vpc/latest/userguide/configure-subnets.html)