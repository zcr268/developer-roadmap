# Lambda

Lambda is AWS's serverless compute service. You upload a function, configure a trigger, and AWS runs the code in response to events without provisioning or managing servers. You pay only for the compute time consumed during execution.

Visit the following resources to learn more:

- [@official@AWS Lambda](https://docs.aws.amazon.com/lambda/latest/dg/welcome.html)