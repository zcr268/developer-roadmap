# Cloudfront

CloudFront is AWS's content delivery network (CDN). It caches content at edge locations around the world, reducing latency for end users by serving requests from the location closest to them. CloudFront works with S3, EC2, load balancers, and custom origins.

Visit the following resources to learn more:

- [@official@Amazon Cloudfront](https://aws.amazon.com/cloudfront/)