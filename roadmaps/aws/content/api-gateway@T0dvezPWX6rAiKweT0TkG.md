# API Gateway

API Gateway is a fully managed service for creating, deploying, and securing APIs at scale. It supports REST, HTTP, and WebSocket APIs and integrates directly with Lambda, making it a common entry point for serverless backends. It handles request routing, authentication, rate limiting, and response transformation.

Visit the following resources to learn more:

- [@official@AWS API Gateway](https://aws.amazon.com/api-gateway/)