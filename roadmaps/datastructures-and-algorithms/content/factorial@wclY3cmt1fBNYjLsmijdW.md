# Factorial

Factorial complexity, O(n!), grows faster than exponential and represents the number of permutations of n elements. Algorithms that generate every possible ordering of a set, such as brute-force permutation search, run in factorial time. This is only feasible for very small inputs.