# Graph Data Structure

Graph search algorithms explore nodes and edges to find paths, detect connectivity, or locate specific nodes. BFS and DFS are the two foundational strategies, each suited to different problems. Unlike tree search, graph search must track visited nodes to avoid infinite loops caused by cycles.