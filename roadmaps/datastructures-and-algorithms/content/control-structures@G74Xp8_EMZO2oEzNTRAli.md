# Control Structures

Control structures direct the flow of execution in a program. They include conditionals like `if/else`, which run code only when a condition is true, and loops like `for` and `while`, which repeat a block of code. Most algorithmic logic depends on these constructs to make decisions and iterate over data.