# Graph Data Structure

A minimum spanning tree (MST) is a subset of a graph's edges that connects all vertices with the minimum total edge weight and no cycles. MSTs are used in network design, clustering, and approximation algorithms. Two classic algorithms for finding them are Kruskal's and Prim's.