# Algorithmic Complexity

Algorithmic complexity describes how the runtime or memory usage of an algorithm grows as the input size increases. It is the primary tool for comparing algorithms and predicting how they will perform at scale. Complexity is typically expressed using Big-O notation, which captures the worst-case growth rate.