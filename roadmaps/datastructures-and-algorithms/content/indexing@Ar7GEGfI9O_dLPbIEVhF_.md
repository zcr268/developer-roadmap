# Indexing

Indexing is a technique for organizing data to speed up retrieval operations. Instead of scanning every record, an index provides a faster path to the data. Different indexing strategies suit different data layouts and query types, and choosing the right index can dramatically reduce query time.