# Functions

A function is a named, reusable block of code that takes inputs, performs a task, and optionally returns a result. Functions let you break a program into smaller, manageable pieces and avoid repeating the same code in multiple places. Most algorithms are expressed as functions, and understanding how they receive arguments and return values is fundamental.