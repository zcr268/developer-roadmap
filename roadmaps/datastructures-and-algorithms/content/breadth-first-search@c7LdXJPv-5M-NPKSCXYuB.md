# Breadth First Search

Breadth-first search on a graph visits all neighbors of a node before moving to the next level, using a queue to track the order. It finds the shortest path in unweighted graphs and can determine whether two nodes are connected. Visited nodes are marked to prevent revisiting.