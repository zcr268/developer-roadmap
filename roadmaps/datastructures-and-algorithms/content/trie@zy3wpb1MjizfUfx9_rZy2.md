# Trie

A trie, also called a prefix tree, is a tree-like structure where each node represents a character, and paths from root to leaf represent strings. It allows fast prefix lookups and is commonly used in autocomplete, spell checkers, and IP routing tables. Search and insertion both run in O(m) time, where m is the length of the string.