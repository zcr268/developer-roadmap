# Tree Data Structures

Tree traversal refers to visiting every node in a tree in a specific order. The three common strategies for binary trees are in-order, pre-order, and post-order, each visiting nodes in a different sequence relative to the root. The correct traversal depends on the operation: in-order produces sorted output from a BST, while pre-order is used for copying a tree.