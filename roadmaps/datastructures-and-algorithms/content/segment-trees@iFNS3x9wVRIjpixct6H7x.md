# Segment Trees

A segment tree is a binary tree used to answer range queries, such as the sum or minimum of elements in a subarray, in O(log n) time. It also supports point and range updates efficiently. Segment trees are common in competitive programming and database query optimization.