# Tree-Based Indexing

Tree-based indexing uses a tree structure, typically a B-tree or B+ tree, to organize index entries. It supports efficient range queries, equality lookups, and sorted access in O(log n) time. Most relational databases use tree-based indexes as their default indexing strategy.