# Tree Data Structures

Tree search algorithms locate a specific node or value within a tree structure. The two primary strategies are breadth-first search, which explores level by level, and depth-first search, which explores as far down a branch as possible before backtracking. The right choice depends on the tree's shape and what is being searched for.