# Fenwick Trees

A Fenwick tree, also called a binary indexed tree (BIT), is a data structure that supports prefix sum queries and point updates in O(log n) time. It is more memory-efficient and simpler to implement than a segment tree for problems involving cumulative sums. It is commonly used in problems requiring frequent updates and sum queries over ranges.