# Database Migrations
 
Database migrations are versioned scripts that define incremental changes to a database schema, such as creating tables, adding columns, or modifying indexes. They allow teams to evolve the database schema in a controlled, reproducible way. Tools like Phinx and Laravel's built-in migrator manage migration history and execution order.