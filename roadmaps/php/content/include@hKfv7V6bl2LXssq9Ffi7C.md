# include
 
`include` inserts the contents of another PHP file into the current script. If the file is not found, PHP emits a warning but continues execution. It is appropriate when the included file is optional and the script can still function without it.

Visit the following resources to learn more:

- [@official@include](https://www.php.net/manual/en/function.include.php)