# echo

`echo` outputs one or more strings to the browser or command line. It is a language construct, not a function, so parentheses are optional. `echo` can accept multiple comma-separated arguments and is marginally faster than `print` because it does not return a value.