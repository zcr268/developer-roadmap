# Service Recovery

Service recovery is the process of restoring a system or service to full operation after an incident. Engineering managers track the recovery plan, remove blockers, and coordinate across teams when recovery involves multiple systems or dependencies. Speed of recovery matters, but so does confidence that the fix is durable.